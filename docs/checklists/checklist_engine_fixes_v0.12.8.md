# Checklist — v0.12.8 engine-level fixes (icons, contrast, dropped CSS)

Goal: fix Servo/Stylo limitations in-tree instead of per-site hacks. Evidence from a 6-site run (speedtest, cloudflare docs, wikipedia, github, ddg, HN): 223 rules dropped for `:has()`, 257 `@media (prefers-reduced-motion)` blocks dropped, `forced-colors`/`prefers-contrast` dropped, `-webkit-text-fill-color` gecko-only, `mask-image` parse-only, `word-break: break-word` / `text-wrap: balance` / `display: -webkit-box` rejected.

## Stylo (vendored at vendor/stylo, upstream rev in UPSTREAM_REV, wired via [patch] in Cargo.toml)
- [x] Backups: backups/servo_real.rs.v0.12.7.bak, toolbar.html.v0.12.7.bak, servo_compat.rs.v0.12.7.bak
- [x] `:has()` + `:nth-child(of)` + `@starting-style` + light-dark images via `stylo_static_prefs::set_pref!` in `servo_prefs()`
- [x] Media features for Servo: prefers-reduced-motion, prefers-reduced-transparency, prefers-contrast, forced-colors, inverted-colors, dynamic-range, -ms-high-contrast (default-state evaluators)
- [x] `text-wrap` / `text-wrap-style` parse under layout.unimplemented
- [x] Build green with vendored Stylo (14m01s); `:has` drops 223 -> 0, media drops 507 -> 23 on the 6-site run
- [x] On-screen: test/engine/features.html — :has box green, all media-feature lines green, gradient headline readable, text-fill teal, -webkit-box row

## Servo (vendored at vendor/servo: components + workspace files, no tests/wpt; 59 crates in [patch."https://github.com/servo/servo.git"], generated by scripts/gen_servo_patch.py)
- [x] `-webkit-text-fill-color` painted for glyphs (stylo engine gate removed; display_list/mod.rs text path)
- [x] `background-clip: text`: layer/colour not painted as a box; transparent fill falls back to first gradient stop / background-color (`background::representative_color`)
- [x] `mask-image: url()` painted via WebRender `define_clip_image_mask` on background layers (`build_mask_clip_chain`, `background::layout_mask`; mask-size/position honoured, repeat + gradient masks ignored)
- [x] `word-break: break-word` / `overflow-wrap: break-word|anywhere` emergency breaking: shaping queue emits per-character `WordFragment` slices when the style allows breaking inside words; `process_weak_wrap_opportunity` (inline/mod.rs) only takes those breaks when the whole word is wider than the line, breaking where the next piece would overflow. Min-content ignores fragment breaks.
- [x] `display: -webkit-box` / `-webkit-inline-box` parse and lay out as flex (row)
- [ ] `-webkit-line-clamp` (still gecko-only)
- [ ] `fill: GrayText` system colors in SVG paint (4 occurrences, deferred)
- [x] Local `file://` subresources: referrer is stripped for local documents, so the gate now also accepts loads when the document is unknown and a content tab is on a file:// URL (`file_subresource_allowed`)
- [x] cargo check green (scripts/check.cmd) with all patches
- [x] Release build green + on-screen check of test/engine/features.html (mask icons, icon fonts, gradient text, :has, media features, break-word, URL wrap, -webkit-box)
- [x] mask-image via WebRender image-mask clip panicked on rects and painted nothing on pictures; replaced by ColorMatrix tint of the mask image
- [x] Gradient masks: paint nothing (no solid box)
- [x] SVG repair: `color(display-p3 …)` -> rgb() (speedtest wordmark visible)
- [x] textarea: rows x line-height minimum applied in layout (`text_area_rows`); presentational em height and `lh` unit both misfired
- [x] requestIdleCallback polyfill as a document-start user script (GitHub error banner)

## Desktop glitches found this lap
- [x] (hardened) Ctrl+digit / Ctrl+Tab activates the focused element on the page being left (HN opened #2 story, Wikipedia submitted search). Trace: keyup of the digit + Control forwarded to content.
- [x] Window position + maximized persisted in session.json (`window_x/window_y/maximized`), restored via `restore_window_position` clamped to the monitor work area (windows-sys MonitorFromPoint/GetMonitorInfoW); no saved position = centered (lands under taskbar / half off-screen)
- [x] Cloudflare docs sidebar chevrons = gray boxes (mask-image) — gone on the v0.12.8 engine (tab3 shot)
- [x] Speedtest wordmark missing (p3 fill)

## Close-out
- [x] cargo check clean; cargo test 199 pass / 2 pre-existing failures (anchor 119px chrome, compat sheet font-stretch) from the uncommitted 0.12.7 chrome work
- [x] architecture_map.md + CHANGELOG.md entries
- [ ] Anthony confirms on his sites
